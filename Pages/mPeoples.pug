html 

    div#all 
        each val in pData 
                -let x = val.id + "s"
                div(id = x)
                    a(href="/people/"+val._id) #{val.name} 
                    button.addPeople(type ="button", id = val.id) ADD PERSON